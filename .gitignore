node_modules/
.wordpress-playground/

# Database files written directly into the repo root (before submodule is set up).
# Once init-data is run, ./data is a submodule and this line has no effect.
data/*.sqlite
data/*.sqlite-shm
data/*.sqlite-wal
